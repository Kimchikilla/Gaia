# Gaia Artifacts

`artifacts`는 사람이 읽는 문서가 아니라 재현 가능한 산출물을 둔다.

| 디렉터리 | 내용 |
|---|---|
| `benchmarks/` | benchmark JSON 결과 |
| `figures/` | 생성된 PNG figure |

문서 요약은 `docs/`에 두고, 논문 원고와 PDF는 `manuscripts/gaia/`에 둔다.
